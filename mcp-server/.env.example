# Same Postgres connection as the main app's .env - this server reads/writes
# the same Store/Order/CatalogItem tables.
DATABASE_URL="postgresql://postgres.ohebfdpyuumoqbqmnjxj:[YOUR-PASSWORD]@aws-0-ap-south-1.pooler.supabase.com:6543/postgres"

# "stdio" for local/CLI testing (default), "http" when deploying for Claude to connect to
TRANSPORT="stdio"

# Only used when TRANSPORT=http
PORT="3000"
